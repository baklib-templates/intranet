<div class="mb-12 p-4 bg-white">
  <div class="mt-5 flex flex-col -my-2.5 space-y-5">
    <div>
      <label for="title" class="block mb-1 font-bold">{{ 'generic.catalog' | t }}</label>
      <select name="page[parent_full_path]" class="border p-2 rounded-md">
        {% for channel in site.pages['/'].children_in_list %}
          <option value="{{ channel.path }}">{{ channel.link_text }}</option>
        {% endfor %}
      </select>
    </div>
    <div>
      <input value="page" autocomplete="off" type="hidden" name="page[template_name]">
      <input value="" autocomplete="off" type="hidden" name="page[template_style]">
      <input value="{{ site.settings.is_allow_published }}" autocomplete="off" type="hidden" name="page[published]">
      <label for="title" class="block mb-1 font-bold">{{ 'generic.title' | t }}</label>
      <input
      type="text"
      name="page[template_variables][title]"
      value="{% if page.id  %}{{ page.link_text }}{% endif %}"
      class="form-input mt-1.5 w-full rounded-lg border-2 py-2" required />
      {%# 提示 %}
    </div>

    {%# 内容 %}
    <div class="">
      <label for="content" class="block mb-1 font-bold">{{ 'generic.content' | t }}</label>
      {% assign content = page.settings.content %}
      {% form_field_tag 'richtext', 'page[template_variables][content]', content, toolbar: 'full' %}
      {% comment %} <textarea name="page[template_variables][content]" rows="12" class="w-full form-textarea placeholder:italic"
      placeholder="问题详情" required>{{ content }}</textarea> {% endcomment %}
    </div>
  </div>
  <div class="my-5 space-x-2">
    <button type="submit" class="py-2 btn">{{ 'buttons.submit' | t }}</button>
    <a href="{{ site.index_path }}" class="btn btn-ghost">{{ 'buttons.cancel' | t }}</a>
  </div>
  <ul class="list-inside list-disc text-sm text-gray-500">
    <li>{{ 'generic.terms1' | t }}</li>
    <li>{{ 'generic.terms2' | t }}<a href="/s/terms" class="text-blue-600 font-semibold">免责声明</a>。</li>
  </ul>
</div>